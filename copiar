directory="/path/to/your/directory"

# Check if there's a directory created in the last 30 seconds
if [ -n "$(find $directory -maxdepth 1 -type d -mmin -0.5)" ]; then
    echo "A directory was created in the last 30 seconds."
else
    echo "No directory was created in the last 30 seconds."
fi
